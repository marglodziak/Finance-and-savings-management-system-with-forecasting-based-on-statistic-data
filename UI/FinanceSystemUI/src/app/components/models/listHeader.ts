export class ListHeader {
    name: string;
    arrowDirection: number;

    constructor(name: string, arrowDirection: number) {
        this.name = name;
        this.arrowDirection = arrowDirection;
    }
}